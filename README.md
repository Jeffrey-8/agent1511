# 🤖 Telegram Bot "Потенциал партнёрской программы"

AI-агент для расчета потенциала партнерской программы банка с использованием GigaChat API.

## 📋 Описание

Telegram-бот автоматически собирает информацию о компании через диалог с пользователем и рассчитывает потенциал продаж по различным сегментам и каналам.

### Возможности:
- ✅ Сбор информации о компании (отрасль, выручка, численность)
- ✅ Автоматическое определение ОКВЭД кодов
- ✅ Интеллектуальный диалог через GigaChat API
- ✅ Расчет потенциала по сегментам и каналам
- ✅ Режим расчета без фильтров (весь рынок)
- ✅ Сохранение истории диалогов в БД

## 🏗️ Архитектура

```
telegram_bot.py              # Главный модуль бота
├── company_info_agent.py    # Сбор информации о компании
├── calculation_params_agent.py  # Сбор параметров расчета
├── revenue_extractor_agent.py   # Извлечение категории выручки
├── okved_agent.py           # Определение ОКВЭД кодов
├── analytics_engine.py      # Расчет потенциала
├── database_manager.py      # Работа с БД
├── reference_data.py        # Справочники
└── results_formatter.py     # Форматирование результатов
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Скопируйте шаблон и заполните значения:

```bash
cp env-template.txt .env
```

Отредактируйте файл `.env` и укажите:
- `TELEGRAM_BOT_TOKEN` - токен от @BotFather
- `GIGACHAT_AUTH` - Base64-encoded credentials GigaChat
- Остальные параметры GigaChat API

### 3. Подготовка базы данных

#### Импорт основных данных:
```bash
python database_manager.py import
```

#### Импорт справочника ОКВЭД:
```bash
python etl_okved.py
```

### 4. Запуск бота

```bash
python telegram_bot.py
```

## 📝 Получение токенов

### Telegram Bot Token

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Укажите название бота
4. Укажите username бота (должен заканчиваться на `bot`)
5. Скопируйте полученный токен

### GigaChat API Credentials

1. Зарегистрируйтесь на [developers.sber.ru](https://developers.sber.ru/)
2. Войдите в личный кабинет
3. Перейдите в раздел "GigaChat API"
4. Создайте новый проект
5. Получите `CLIENT_ID` и `CLIENT_SECRET`
6. Закодируйте в Base64:

**Linux/Mac:**
```bash
echo -n "YOUR_CLIENT_ID:YOUR_CLIENT_SECRET" | base64
```

**Windows PowerShell:**
```powershell
[Convert]::ToBase64String([Text.Encoding]::ASCII.GetBytes("YOUR_CLIENT_ID:YOUR_CLIENT_SECRET"))
```

7. Полученную строку вставьте в `GIGACHAT_AUTH`

## 🎯 Использование

### Команды бота:

| Команда | Описание |
|---------|----------|
| `/start` | Начать новый диалог |
| `/reset` | Сбросить текущий диалог |
| `/no_filters` | Расчет по всему рынку без фильтров |
| `/help` | Справка о боте |

### Пример диалога:

```
Пользователь: IT компания, 50 человек, выручка 100 млн
Бот: ✅ Отлично! Информация собрана.
     🔍 Определяю ОКВЭД коды...
     
Бот: Теперь нужны параметры расчета...
Пользователь: Чек ММБ 50000, другие 30000, кприб 10%, доля 50%, коробка

Бот: ⏳ Выполняю расчет...
     📊 Потенциал по сегментам:
     • ММБ: 22.84 млн руб.
     ...
```

## 📊 Структура базы данных

### data_storage.db

**Таблица `data_table`:**
- Основные бизнес-данные
- Поля: сегменты, ОКВЭД, выручка, штат, ТБ
- Метрики: `fact_amt`, `fact_amt_2`, `field_11_value_n`

**Таблица `okved`:**
- Справочник ОКВЭД кодов
- Поля: `code`, `name`

**Таблица `chat_sessions`:**
- История диалогов
- Поля: `chat_id`, `session_id`, `user_response`, `company_info`, `revenue_category`

## 🔧 Разработка

### Структура проекта:

```
agent1511_backup_2025-11-27_01-53/
├── telegram_bot.py              # Главный файл
├── company_info_agent.py        # AI-агент сбора информации
├── calculation_params_agent.py  # AI-агент параметров
├── revenue_extractor_agent.py   # Извлечение выручки
├── okved_agent.py              # Определение ОКВЭД
├── analytics_engine.py         # Расчет потенциала
├── database_manager.py         # Менеджер БД
├── reference_data.py           # Справочники
├── results_formatter.py        # Форматирование
├── etl_okved.py               # ETL для ОКВЭД
├── data_storage.db            # База данных
├── okved.xlsx                 # Справочник ОКВЭД
├── prompts.xlsx               # Промпты (опционально)
├── env-template.txt           # Шаблон .env
├── requirements.txt           # Зависимости
├── .env                       # Переменные окружения (не в git)
├── .gitignore                 # Исключения для git
└── README.md                  # Документация
```

### Запуск тестов:

```bash
# Тест агента компании
python company_info_agent.py

# Тест агента выручки
python revenue_extractor_agent.py

# Тест ОКВЭД агента
python okved_agent.py

# Тест расчетов
python analytics_engine.py
```

### Работа с БД:

```bash
# Интерактивный режим SQL
python database_manager.py query

# Показать статистику
python database_manager.py stats

# Подключиться к identifier.sqlite
python database_manager.py identifier
```

## 📚 Справочники

### Сегменты и каналы:

| Сегмент | Каналы | Мин. клиентов |
|---------|--------|---------------|
| ММБ | ММБ МКК, ЦКР, КМ МС | 105K-125K |
| СКМ | КМ и ТКМ СКМ | 10 |
| РГС | КМ и ТКМ РГС | 0 |
| КСБ | КМ ККСБ | 2700 |
| KeyClients | DTO | 1 |

### Категории выручки:
- Менее 1 млн.р.
- 1-10 млн.р.
- 10-120 млн.р.
- 120-800 млн.р.
- Более 800 млн.р.

### Категории штата:
- 1 чел.
- 2-5 чел.
- 6-30 чел.
- 31-100 чел.
- Более 100 чел.

## 🔒 Безопасность

- ⚠️ **Не загружайте `.env` файл в git!**
- ⚠️ **Не делитесь токенами публично**
- ✅ Используйте `.gitignore` для исключения конфиденциальных файлов
- ✅ Регулярно обновляйте зависимости

## 📖 Дополнительная информация

### Логирование

Бот использует стандартное логирование Python:
```python
import logging
logging.basicConfig(level=logging.INFO)
```

Все агенты логируют свою работу для отладки.

### Обработка ошибок

- Retry-логика для сетевых запросов
- Fallback при ошибках API
- Парсинг JSON с очисткой и восстановлением
- Разбиение длинных сообщений (> 4096 символов)

### Кастомизация

Параметры в `reference_data.py`:
- `MIN_CLIENTS` - минимальное количество клиентов
- `CHANNEL_COSTS` - себестоимость каналов
- `SEGMENT_CHANNELS` - каналы для сегментов
- Справочники категорий

## 🤝 Поддержка

При возникновении проблем проверьте:
1. ✅ Все ли зависимости установлены
2. ✅ Правильно ли заполнен `.env`
3. ✅ Доступны ли API endpoints
4. ✅ Импортированы ли данные в БД
5. ✅ Логи бота для диагностики ошибок

## 📄 Лицензия

Проект разработан для внутреннего использования.

---

**Версия:** 1.0  
**Дата:** 2025-11-27

